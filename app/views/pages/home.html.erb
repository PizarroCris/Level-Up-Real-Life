<div class="container p-3">
  <div class="d-flex justify-content-between">
    <div class="text-danger fs-3"><%= "LEVEL" %></div>
    <div class="text-danger fs-3"><%= "EXP" %></div>
    <div class="text-danger fs-3"><%= "STEPS" %></div>
  </div>

  <div class="container fixed-bottom p-3">
    <div class="d-flex justify-content-between">
      <%= link_to "Inventory", "inventory_path", class:"btn btn-primary rounded-5" %> <!-- mudar o path dos botoes quando tiver a rota criada -->
      <div class="d-flex gap-2">
        <%= link_to "Menu", "menu_path", class:"btn btn-primary rounded-5" %> <!-- mudar o path dos botoes quando tiver a rota criada -->
        <%= link_to "Shop", "shop_path", class: "btn btn-primary rounded-5" %> <!-- mudar o path dos botoes quando tiver a rota criada -->
      </div>
    </div>
  </div>
</div>
